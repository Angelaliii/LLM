🚀 歷史人物對話系統 - 48小時衝刺執行計劃
🟥 第一階段：最高優先級 (High Priority)
目標：實作「內部雙過程理論」，解決 NPC 爆雷問題，確保系統流暢。 時間預估：4-6 小時

1. [核心] 實作 Internal Monologue (<thinking>)
這是你的專題亮點，也是解決「警察太容易給答案」的關鍵。

行動：修改 buildSystemPrompt 函式中的 [OUTPUT_SCHEMA]。

內容：強制模型在 <thinking> 標籤中先進行：意圖偵測、禁語檢查、人設校準、反問策略。

程式碼位置：src/mistralClient.ts (或你的 prompt 生成位置)。

關鍵點：確保 System Prompt 裡明確寫出：「你必須先 <thinking>，再 <reply>」。

2. [前端] 隱藏思考過程
不要讓國中生看到那一長串的 <thinking>，會破壞沉浸感。

行動：在 API 回傳給前端之前，使用 Regex 清理內容。

程式碼：

TypeScript

// 在 handleGameChat 回傳前
const cleanContent = rawContent.replace(/<thinking>[\s\S]*?<\/thinking>/g, '').trim();
3. [數據] 撰寫「合成範例 (Synthetic Few-Shot)」
解決「找不到古人說話語氣」的問題，不要用 RAG 找古文，直接自己寫。

行動：為三位 NPC（警察、學生、測量員）各寫 3 組對話範例 (user -> <thinking> -> <reply>)。

內容：包含一組「普通對話」、一組「被問到禁語（六三法）的反問應對」、一組「展現傲慢/無知/專業的語氣」。

實作：將這些範例直接硬編碼 (Hard-code) 在 buildSystemPrompt 的 [Examples] 區塊中。

🟧 第二階段：中級優先級 (Medium Priority)
目標：優化判定邏輯，避免使用者因為「打錯字」或「同義詞」卡關。 時間預估：3-4 小時

1. [邏輯] 放寬關鍵字判定 (Fuzzy Matching)
目前的 checkKeyPointAchieved 可能太嚴格，國中生挫折感會很重。

行動：擴充 KEYWORDS 列表，加入口語化同義詞。

範例：

權力 -> [權力, 權利, 老大, 說了算, 很兇, 管很寬]

六三法 -> [六三法, 63法, 總督命令, 不用議會, 直接抓人]

實作：修改 checkKeyPointAchieved 中的陣列比對邏輯，只要命中同義詞庫中的任一個就算得分。

2. [效能] 非同步化記憶濃縮 (Async Summarization)
目前的 summarizeConversation 如果卡在主流程，對話會變慢。

行動：將記憶濃縮改成「射後不理 (Fire-and-Forget)」。

程式碼：

TypeScript

// 原本是 await summarizeConversation(...)
// 改成：
summarizeConversation(...).catch(err => console.error(err));
// 直接 return response 給前端，不用等上面跑完
3. [邏輯] 優化外部審查 (parseAndValidate)
配合第一階段的 <thinking>，更新外部檢查規則。

行動：確保 Parser 懂得檢查 <thinking> 是否存在。如果模型偷懶沒思考直接回答，視為違規，強制重寫。

規則新增：if (!response.includes('<thinking>')) issues.push('missing_internal_monologue');

🟦 第三階段：低優先級 / 未來展望 (Low Priority / Future Work)
目標：錦上添花，或寫在報告的「未來展望」中。 時間預估：專題結束後 / 行有餘力再做

1. [UI] 視覺化思考狀態
概念：雖然隱藏了文字，但在介面上顯示「佐藤警官正在思考...」或一個閃爍的腦袋圖示。這能增加「AI 真的在想」的感覺。

2. [邏輯] 引入 LLM-as-a-Judge (隱形 DM)
概念：如同我們先前討論的，用 LLM 來取代 checkKeyPointAchieved 的關鍵字比對。這能讓判定更靈活（例如：「雖然沒說關鍵字，但他理解是對的」）。

策略：如果還有時間，可以寫一個簡單的 Prompt 試試看；如果沒時間，就寫在報告裡說這是下一步優化方向。

3. [內容] 增加「主動事件」
概念：如果玩家卡在同一個話題太久（例如 > 10 回合），NPC 主動拋出新話題：「對了，你有聽說最近土地測量的事嗎？」。