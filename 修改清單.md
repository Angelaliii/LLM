# 修改清單 (優先順序與說明)

此文件彙整先前分析中發現的問題、優先順序與建議修正步驟，供開發者依序實作與驗證。

---
## 高優先（短期內實作）
角色
你是一位資深 Node.js/TypeScript 後端工程師與程式庫維護者，熟悉高併發 I/O、快取策略、LLM 推論管線設計與可觀測性。你的任務是對現有專案進行精準且可回溯的重構，僅修改必要範圍，並提供可驗證的成果。

專案與影響範圍
目標檔案：
1) backend/services/gameService.ts
2) backend/services/npcConfigManager.ts（若需擴展設定）
資料來源：
- NPC persona 檔位於 backend/data/persona/*.md

終極目標
A. 將 NPC persona 載入流程改為快取（避免每次 fs.readFileSync），支援 lazy-load + TTL，並可選擇啟動時預載（preload）。
B. 在 handleGameChat 內當 checkResponseQuality(hasIssues===true) 時，執行「低溫重生 → 失敗則 rewrite 修正」的品質回退（fallback）策略，策略可經由 npcConfig 配置。

產出品質標準（可量測）
- I/O：避免同步 I/O；persona 首次載入使用非同步 fs/promises，後續命中快取不觸發磁碟讀取（以記錄計數驗證）。
- 延遲：重啟 server 後，首次 /api/game/chat 包含 persona 冷讀；後續同 persona 的請求應顯著降低延遲（以日誌對比首包/後包耗時）。
- 一致性與安全：在高併發同 persona 多請求時，僅進行一次實際讀檔（in-flight promise 去重）。
- 可配置：TTL、是否預載、低溫溫度、重試次數、是否啟用 rewrite 皆可於 npcConfig 設定，並有合理預設值。
- 可觀測性：新增可讀的結構化日誌（層級：info/debug/warn），記錄命中/失效/重載、低溫重生觸發、rewrite 觸發與結果摘要（不要記錄機密內容）。
- 回退策略正確性：當 hasIssues=true → 先低溫重生（可多次）→ 若仍不合格 → rewrite；每步驟都保留原始回應於 metadata。
- 程式風格：TypeScript 嚴格型別，無 any 漏網；函式純度與副作用邊界清晰；錯誤處理完整（try/catch、時間界限與取消語意）。
- 測試：單元測試覆蓋 persona 快取 TTL/併發/失效，以及品質回退流程（以 mock LLM 回傳不合格→合格）。

實作要求 A：NPC persona 快取
1) 設計快取介面
interface PersonaCache {
  get(personaId: string): Promise<{ id: string; content: string }>;
  preloadAll?(): Promise<void>;
  stats(): { hits: number; misses: number; loadsInFlight: number };
  invalidate(personaId?: string): void; // 若無參數則全清
}

2) 預設設定（可由 npcConfig 覆寫）
personaCache: {
  strategy: 'lazy' | 'preload';   // 預設 'lazy'
  ttlMs: number;                   // 預設 5 * 60 * 1000
  preload: boolean;                // 預設 false（與 strategy 區分，保留向後相容）
  watchFs: boolean;                // 預設 false（若 true，檔變動即失效）
}

3) 內部結構
- 使用 Map<string, { content: string; expiresAt: number; mtimeMs: number }> 儲存。
- 使用 inFlight Map<string, Promise<Entry>> 避免高併發重複讀檔。
- 檔案路徑：backend/data/persona/${personaId}.md（若現有命名不同，兼容以 glob 掃描建立 id→path 索引）。
- 檔案讀取：fs/promises.readFile + fs/promises.stat（取得 mtimeMs），嚴禁 readFileSync。
- 命中判定：未過期且存在則直接回傳；過期時觸發背景刷新（可選），先回舊值或等刷新完成（採一致性優先，等刷新完成）。
- 失效：手動 invalidate 或 watchFs 觸發時清除對應 key。

4) 啟動預載（可選）
- 若 npcConfig.personaCache.strategy === 'preload' 或 npcConfig.personaCache.preload === true：
  - 啟動時讀取 backend/data/persona/*.md，全部載入至快取。
  - 預載失敗不阻塞服務啟動，但需 warn 並繼續 lazy 補載。

5) 併發與錯誤處理
- 同 persona 多個請求同時來時，共享一個 in-flight promise。
- 讀檔錯誤：reject 並記錄 warn，避免快取污染；後續請求可再嘗試。

6) 觀測與記錄
- stats() 暴露 hits/misses/loadsInFlight。
- 日誌：cache_hit、cache_miss、cache_reload、cache_invalidate、cache_watch_event。

7) 對現有程式碼的變更
- 將 gameService.ts 的 loadNPCPersona 改為使用 PersonaCache.get(personaId)。
- 移除 fs.readFileSync；統一為非同步流程。

實作要求 B：品質不良時自動低溫重生與 rewrite
1) 設定擴展（npcConfigManager.ts）
qualityFallback: {
  enabled: boolean;                 // 預設 true
  lowTemp: number;                  // 預設 0.3（範圍 0.2～0.4）
  maxLowTempRetries: number;        // 預設 1（可設 0～3）
  useRewrite: boolean;              // 預設 true
  maxRewritePasses: number;         // 預設 1
}

2) handleGameChat 流程（偽碼）
const result1 = await callLLM({ temperature: normalTemp, prompt, ... });
const quality1 = checkResponseQuality(result1);
if (!quality1.hasIssues) return result1;

let attempt = 0;
let final = result1;
let quality = quality1;

// 低溫重生（最多 maxLowTempRetries 次）
while (npcConfig.qualityFallback.enabled &&
       attempt < npcConfig.qualityFallback.maxLowTempRetries) {
  attempt++;
  const lowTempOut = await callLLM({ temperature: npcConfig.qualityFallback.lowTemp, prompt, ... });
  const lowTempQ = checkResponseQuality(lowTempOut);
  recordAttempt('low_temp_regen', { attempt, ok: !lowTempQ.hasIssues });
  if (!lowTempQ.hasIssues) { final = lowTempOut; quality = lowTempQ; break; }
}

// 若仍不合格且啟用 rewrite
if (npcConfig.qualityFallback.enabled &&
    npcConfig.qualityFallback.useRewrite &&
    quality.hasIssues) {
  const rewritePrompt = buildRewritePrompt({
    original: final,
    issues: quality.issues,        // 由 checkResponseQuality 提供
    npcRules, npcPersonaVoice
  });
  for (let i = 0; i < npcConfig.qualityFallback.maxRewritePasses; i++) {
    const rewritten = await callLLM({ temperature: Math.min(0.5, npcConfig.qualityFallback.lowTemp), prompt: rewritePrompt, ... });
    const rewrittenQ = checkResponseQuality(rewritten);
    recordAttempt('rewrite', { attempt: i + 1, ok: !rewrittenQ.hasIssues });
    if (!rewrittenQ.hasIssues) { final = rewritten; quality = rewrittenQ; break; }
  }
}

// 最終返回 final，同時把原始不合格回應與品質報告附加到 metadata（供稽核）
return withMetadata(final, {
  originalCandidate: result1,
  fallbackAttempts: collectedAttempts, // 包含每次嘗試的摘要
  qualityReport: quality
});

3) buildRewritePrompt 樣板（請實作成函式）
系統約束（系統訊息）：
- 你正在修正一段 NPC 回應，需嚴格遵守既有世界觀、NPC persona、語氣與對話上下文。不得引入新設定或違反對話規範。
使用者訊息（user）：
- 原始回應（可能不合格）：
<<<BEGIN_ORIGINAL>>>
{originalText}
<<<END_ORIGINAL>>>
- 已檢出的問題：
{bulletListOfIssues}
- 請在不改變既有事實與劇情邏輯的前提下，重寫為合格版本：
要求：
1) 保留 NPC 語氣與人設；禁止破壞世界觀或添加未授權背景。
2) 移除冗長、含糊、重複或違規段落；補足缺漏之必要資訊。
3) 優先輸出清晰、自然、上下文一致的回應。
4) 僅輸出最終回應正文，不要附加任何說明或標籤。

4) 日誌與遙測
- 記錄：quality_issue_detected、low_temp_regen_start/finish、rewrite_start/finish、quality_ok。
- 為每次嘗試記錄溫度、token 使用量摘要（若可取得）、耗時與合格與否（不記錄使用者敏感內容）。
- 以高層次 metrics 暴露成功率（低溫→rewrite 的命中率）。

介面與型別（示例，請據實整合）
type QualityReport = { hasIssues: boolean; issues: string[]; score?: number };
type ChatOutput = { text: string; meta?: Record<string, unknown> };

function checkResponseQuality(o: ChatOutput): QualityReport; // 已存在：保持簽章，若擴充請就地增強

function buildRewritePrompt(args: {
  original: ChatOutput | string;
  issues: string[];
  npcRules: string;
  npcPersonaVoice: string;
}): string;

可配置來源與預設
- npcConfigManager.ts 增加：
{
  personaCache: {
    strategy?: 'lazy' | 'preload';
    ttlMs?: number;
    preload?: boolean;
    watchFs?: boolean;
  },
  qualityFallback: {
    enabled?: boolean;
    lowTemp?: number;
    maxLowTempRetries?: number;
    useRewrite?: boolean;
    maxRewritePasses?: number;
  }
}
- 預設值如前所述；支援以環境變數覆寫（例如 NPC_PERSONA_CACHE_TTL、NPC_PRELOAD_PERSONAS），但以 npcConfig 為最終來源。
- 若不想擴及 env，僅於 npcConfig 支援即可。

對比示例（正例/反例）
正例：同 persona 兩個並發請求同時到達時，僅一次實際讀檔；後一請求等待 in-flight 完成即獲取內容（記錄 cache_miss 1 次）。
反例：每次請求都呼叫 readFileSync，導致 I/O 堵塞，或兩個並發請求各自讀檔兩次（記錄兩次 miss 與讀檔）。

正例：hasIssues=true → 低溫重生一次獲得合格輸出 → 結束流程。
反例：hasIssues=true → 直接 rewrite，跳過低溫重生；或 rewrite 後仍不檢查品質就返回。

邊界情況與保護欄
- persona 檔案不存在：回傳清楚錯誤（NotFound）並記錄 warn；不要以空字串遮蔽錯誤。
- TTL 到期後瞬時高併發：允許一個刷新 in-flight，其餘請求共享該 promise。
- checkResponseQuality 回傳不確定：以保守策略視為 hasIssues=true。
- LLM 回覆空或無效：立即觸發 rewrite；若仍失敗，回傳帶錯誤訊息與原始候選於 metadata（避免資料丟失）。

測試與驗收（請新增/更新測試）
單元測試（Jest）
1) PersonaCache
- 首次 miss 讀檔，後續 hit；驗證命中率與未重複讀檔。
- TTL 到期後刷新；驗證 in-flight 去重（使用 fake timers 與 Promise barrier）。
- invalidate 與（可選）watchFs 行為。
2) 品質回退
- 模擬 LLM 先回不合格、低溫後合格：應停止於低溫步驟。
- 模擬低溫仍不合格、rewrite 後合格：應返回 rewrite 結果，metadata 保留過程。
- 模擬全失敗：回傳最後候選，hasIssues=true，metadata 含完整嘗試記錄與錯誤。

手動驗收
- 重啟 server 後，對相同 NPC 連續呼叫 /api/game/chat；比較首包與後包延遲，應顯著下降（在日誌中可見 cache_miss→cache_hit）。
- 觸發品質問題樣例，觀察日誌中 low_temp_regen 與 rewrite 觸發與成效。

交付物
- 已修改的 gameService.ts 與 npcConfigManager.ts
- 新增 PersonaCache 實作（可置於 backend/services/personaCache.ts），並於 gameService.ts 使用
- 單元測試檔（__tests__/personaCache.test.ts、__tests__/qualityFallback.test.ts）
- 變更紀錄（簡短 README 或 PR 描述）包含設定項與操作說明

自我驗證（提交前執行）
1) 逐行檢查是否仍存在任何同步 I/O。
2) 關閉快取（invalidate 全部）後，連續兩次請求是否只在第一次觸發讀檔。
3) 人工製造不合格輸出：是否依序觸發低溫重生→rewrite，且每步皆重檢品質。
4) 移除/改名 persona 檔案：是否得到清晰錯誤，並不污染快取。
5) 測試全數通過，且日誌中能看到命中率與回退路徑。


## 中優先（短期內實作）

角色
你是一位資深 Node.js/TypeScript 後端工程師與程式架構師，熟悉 Express 路由、型別安全配置管理、可觀測性與 LLM 提示詞工程。你的任務是針對現有專案進行「最小侵入」且可回溯的實作，維持相容性並補足測試。

專案範圍
受影響檔案：
- backend/services/npcConfigManager.ts
- backend/services/gameService.ts
- backend/routes/game.ts（或新增單獨 persona 路由檔，並在 app.ts/route index 中掛載）

既有前提
- 已實作 PersonaCache（快取 persona 檔案）並由 gameService 使用（若尚未存在，請先 stub 出 get() 與 stats() 以便本次工作可運行）。
- 已存在 checkResponseQuality 與 handleGameChat。

終極目標
A) 把 temperature 引入 NPC 設定：於 npcConfigManager 新增 language.preferredTemperature，handleGameChat 使用該值作為主要候選的溫度（並與品質回退機制的低溫邏輯一致整合）。
B) 新增 GET /api/persona/:npcId：可從快取或底層檔案回傳 persona 內容，支援最小化、內部使用與存取控制。
C) 改善 System Prompt 排序與「必須遵守」規則的強制性：將 MUST 規則置頂且結構化，回應後使用 parser 驗證；若違規則走 rewrite。

可量測與驗收標準
1) 配置驅動的溫度：不同 NPC 可擁有不同 language.preferredTemperature；handleGameChat 不再硬編碼 0.7/0.9。當缺省時使用合理預設（例如 0.7）並夾在 [0.1, 1.0] 範圍。
2) 路由可用性：GET /api/persona/:npcId 能正常返回 persona；可選擇來源為 cache 或 file；具備明確錯誤語意（404 不存在、503 快取不可用等）。
3) 安全性與敏感性：路由可由設定開關；預設僅在非 production 或具備權杖時啟用；回應可選擇是否返回完整內容或摘要（由 config 控制）。
4) Prompt 強制性：System Prompt 重新排序後，品質檢查可偵測 MUST 規則違反；若違反，進入 rewrite；最終輸出通過 parser。
5) 可觀測性：增加結構化日誌與 metrics，記錄 temperature 決策來源（npcConfig 或預設）、persona 路由命中來源（cache/file）、parser 合規與 rewrite 啟用狀態。
6) 測試覆蓋：新增/更新測試包含：配置溫度讀取與夾值、不同 NPC 的溫度生效；persona 路由在開關開/關、cache/file 來源、不存在檔案與錯誤路徑；System Prompt MUST 與 parser 驗證觸發 rewrite 的流程。

實作詳述

一、把 temperature 引入 NPC 設定
1) 配置結構（npcConfigManager.ts）
export type NpcLanguageConfig = {
  preferredTemperature?: number; // 建議範圍 0.3–0.8，若越界則夾值
};

export type NpcConfig = {
  id: string;
  language?: NpcLanguageConfig;
  // ...既有欄位（personaCache、qualityFallback 等）
};

export function getNpcConfig(npcId: string): NpcConfig { ... }
export function getNpcTemperature(npcId: string, fallback = 0.7): number {
  const t = getNpcConfig(npcId)?.language?.preferredTemperature ?? fallback;
  const clamped = Math.min(1.0, Math.max(0.1, t));
  if (clamped !== t) log.warn({ npcId, t, clamped }, 'npc_temperature_clamped');
  return clamped;
}

2) handleGameChat 溫度取用（gameService.ts）
- 主候選呼叫 callLLM 時的 temperature = getNpcTemperature(npcId)。
- 品質回退低溫邏輯沿用既有 qualityFallback.lowTemp；若未設置，則從主溫度推導為 Math.max(0.2, roundToOneDecimal(getNpcTemperature(npcId) - 0.4))，上限不高於主溫度，最低 0.2。
- 日誌：記錄 { npcId, tempPrimary, tempLow }。

二、新增 GET /api/persona/:npcId
1) 目的與回應
- 用於開發除錯或內部工具查看 persona 內容。
- 預設僅在非 production 或啟用顯式開關時可用。

2) 設定擴展（npcConfigManager.ts 或全域 config）
export type PersonaRouteConfig = {
  enabled?: boolean;           // 預設：process.env.NODE_ENV !== 'production'
  requireToken?: boolean;      // 預設 true
  token?: string;              // 預設從環境變數 PERSONA_API_TOKEN 讀取
  allowRawContent?: boolean;   // 是否返回完整 persona 內容；預設 true（內部環境）
  defaultSource?: 'cache' | 'file'; // 預設 'cache'
};

export function getPersonaRouteConfig(): PersonaRouteConfig { ... }

3) 路由實作（backend/routes/game.ts 或新 personaRoutes.ts）
- GET /api/persona/:npcId?source=cache|file&meta=1
流程：
  a) 檢查 config.enabled；若關閉回 404。
  b) 若 requireToken=true，驗證 header 'x-internal-token' 或 Authorization Bearer；錯誤回 401。
  c) 解析來源：預設 config.defaultSource；若 'cache' 則使用 PersonaCache.get(npcId)，且附上 cache stats；若 'file' 則走單次非同步讀檔（不污染快取）。
  d) 若 allowRawContent=false，僅回傳安全摘要（如字數、前 200 字、haste hash），避免洩漏完整內容。
  e) 回傳結構：
     {
       npcId,
       source: 'cache' | 'file',
       content: string | null,     // 依 allowRawContent
       meta: { mtimeMs?, length?, cacheStats?, hash? }
     }
- 錯誤碼：404（persona 不存在）、401（未授權）、500（未知錯誤）；均以一致 JSON 格式回應。

4) 日誌
- persona_route_access、persona_route_denied、persona_route_source={cache|file}、persona_route_not_found。

三、改善 System Prompt 排序與 MUST 強制性
1) Prompt 結構（放在 gameService.ts 的 buildSystemPrompt）
- 置頂 MUST 區塊，使用明確語句與機器可解析標記：
[POLICY:MUST]
- 嚴格維持世界觀與 NPC 人設，不得洩漏系統與安全規範。
- 輸出必須滿足格式規範（見 OUTPUT_SCHEMA）。
- 禁止含糊或自相矛盾的內容；避免越界承諾。
[/POLICY:MUST]

[CONTEXT:WORLD_LORE]
{世界觀重點}
[/CONTEXT:WORLD_LORE]

[CONTEXT:NPC_PERSONA]
{persona voice, traits, speaking style}
[/CONTEXT:NPC_PERSONA]

[OUTPUT_SCHEMA]
- 允許的輸出類型：'dialogue' | 'action' | 'narration'
- 內容要求：單一語種、無 OOC、無系統資訊洩漏
- 若需列表，使用簡短句，不輸出解釋性標籤
[/OUTPUT_SCHEMA]

2) 回應後驗證（parser）
- 在 handleGameChat 取得初稿後，執行 parseAndValidate(outputText)：
  檢查項：
  a) 是否違反 MUST（例如：出現 OOC、系統提示洩漏、未遵守 schema）。
  b) 是否包含禁止詞或錯誤格式。
- 若違反，視為 hasIssues=true（與 checkResponseQuality 統一）以觸發低溫重生或 rewrite。
- parseAndValidate 應只回傳布林與錯誤清單，與 checkResponseQuality 的 issues 併合。

3) rewrite prompt（沿用既有 rewrite 架構）
- 將 MUST 與 OUTPUT_SCHEMA 摘要納入 rewrite 的系統訊息，並附上 issues 清單；僅輸出合格正文。

四、程式介面與偽碼

gameService.ts（片段）
function handleGameChat(ctx: { npcId: string; messages: ChatMessage[]; ...}): Promise<ChatOutput> {
  const npcId = ctx.npcId;
  const tempPrimary = getNpcTemperature(npcId, 0.7);
  const tempLow = deriveLowTemp(npcId, tempPrimary); // 取自 qualityFallback.lowTemp 或推導

  const systemPrompt = buildSystemPrompt({ npcId, persona: await personaCache.get(npcId), ... });

  const primary = await callLLM({ temperature: tempPrimary, system: systemPrompt, messages: ctx.messages });
  let report = mergeQualityReports(checkResponseQuality(primary), parseAndValidate(primary.text));
  if (!report.hasIssues) return withMeta(primary, { temp: tempPrimary, quality: report });

  log.info({ npcId, tempPrimary, tempLow }, 'quality_issue_detected_primary');

  // 低溫重生（遵循既有 qualityFallback 設定）
  const regen = await maybeLowTempRegen({ tempLow, systemPrompt, messages: ctx.messages });
  if (regen.ok) return withMeta(regen.output, { temp: tempLow, quality: regen.report, fallback: 'low_temp' });

  // rewrite
  const rewritten = await maybeRewrite({ original: regen.last ?? primary, issues: regen.report.issues, systemPrompt, npcPersonaVoice: ... });
  return withMeta(rewritten.output, { fallback: 'rewrite', attempts: rewritten.attempts, quality: rewritten.report });
}

npcConfigManager.ts（片段）
export function getNpcTemperature(npcId: string, fallback = 0.7): number { ... }
export function getPersonaRouteConfig(): PersonaRouteConfig { ... }

routes（片段）
router.get('/api/persona/:npcId', async (req, res) => {
  const cfg = getPersonaRouteConfig();
  if (!cfg.enabled) return res.status(404).json({ error: 'persona_route_disabled' });
  if (cfg.requireToken && !isAuthorized(req, cfg)) return res.status(401).json({ error: 'unauthorized' });

  const source = (req.query.source as 'cache' | 'file') || cfg.defaultSource || 'cache';
  const npcId = req.params.npcId;

  try {
    const result = await readPersona(npcId, source); // 封裝：cache.get 或 fs/promises.readFile
    if (!result) return res.status(404).json({ error: 'persona_not_found', npcId });
    const payload = buildPersonaPayload(result, { allowRaw: cfg.allowRawContent });
    res.json(payload);
    log.info({ npcId, source, length: payload.meta?.length }, 'persona_route_ok');
  } catch (err) {
    log.warn({ npcId, source, err: serializeErr(err) }, 'persona_route_error');
    res.status(500).json({ error: 'persona_route_error' });
  }
});

五、安全與權限
- 預設在 production 關閉 persona 路由；開啟時需 token。
- 嚴禁將使用者輸入直接插入 System Prompt 的 MUST 段落；進行必要的 escape。
- 日誌不記錄 persona 全文或使用者訊息內容，只記錄長度與雜湊。

六、測試（Jest）
- npcConfigManager：不同 NPC 的 preferredTemperature 讀取與越界夾值；預設值生效。
- gameService：主溫度取自 config；當 parser 檢出 MUST 違反時能觸發低溫重生與 rewrite；成功後 quality.hasIssues=false。
- persona 路由：在 enabled=false 時 404；無 token 時 401；cache 與 file 來源皆可返回；不存在檔案時 404；allowRaw=false 僅返回摘要。
- 快照或樣例：System Prompt MUST 段落位於最前、OUTPUT_SCHEMA 存在。

七、對比示例
正例：NPC A 設 0.35、NPC B 設 0.75；A 的回覆較穩定簡潔、B 的回覆較具探索性，並在日誌中可見各自決策溫度。
反例：忽略配置，仍硬編碼 0.7 或 0.9；或在 production 無授權暴露 persona 全文。

交付物
- 修改後的 npcConfigManager.ts、gameService.ts
- 新增或修改 backend/routes/game.ts（或新 personaRoutes.ts）
- 測試檔：__tests__/npcTemperature.test.ts、__tests__/personaRoute.test.ts、__tests__/promptEnforcement.test.ts
- 變更紀錄（PR 描述/README 更新）：新增配置鍵、路由使用方法、安全注意事項、測試指令

自我檢查清單
- 是否移除硬編碼溫度並以配置替代？越界是否夾值與記錄？
- persona 路由是否預設在 production 關閉？授權是否可設定？
- System Prompt 的 MUST 是否置頂且可機器檢查？parser 是否與品質檢查併合？
- 異常與錯誤碼是否一致且不洩漏敏感內容？
- 測試是否覆蓋主要路徑與錯誤路徑？


## 低優先（可排期）

- **集中管理 mission 資料（後端 API）**
  - 問題：前端 `frontend/app/data/missions` 與後端 `backend/data/missions` 存在重複，容易不同步。
  - 建議：將 canonical mission 数据移至後端並提供 API（`/api/missions/:id`），前端改為 fetch。分階段移植以降低風險。

- **新增診斷日誌與問題回應追蹤**
  - 建議：當 `checkResponseQuality` 發現問題時，寫入一份日誌或儲存到簡易資料庫（包含: sessionId, npcId, 原始回應, issues, context），以利分析與自動化修復流程。

- **增加 chat flow 自動化測試**
  - 建議：為 `buildSystemPrompt`、`checkResponseQuality`、`handleGameChat` 撰寫單元/整合測試，模擬常見違規情況並驗證 fallback 行為。

- **掃描並修正前端 DOM nesting 警告**
  - 已修正一個 `S1_FileDecryption.tsx` 的 `<p>` 包 `<div>` 問題，建議全 repository grep 一次 `"<p>"` 包裝 block-level 元素的情形並修正。

## 實作範例與測試指引（最小可行流程）

1. 快取 persona：
   - 在 `gameService.ts` 新增 `const personaCache: Record<string,string> = {}`，在 module 載入時或在一個 `initPersonas()` 函式中讀取 `data/persona` 下的所有 md 存進快取。
   - 把 `loadNPCPersona` 改為回傳快取內容（若不存在才嘗試讀檔）。

2. 在 `npcConfigManager` 新增 `language.preferredTemperature`（例如 0.3～0.8 範圍），更新現有 config 資料。

3. 在 `handleGameChat`：
   - 使用 `preferredTemperature` 產生 primary response；
   - 當 `checkResponseQuality.hasIssues` 時，先以 `lowTemp = Math.max(0.2, preferredTemperature - 0.3)` 重生一次；
   - 若仍有 issues，呼叫 `rewrite` 流程：將原回應與 `must` 規則送去 LLM 要求改寫（低溫、明確指令）。

4. 新增 `GET /api/persona/:npcId`：
   - 簡單 route 回傳 `personaCache[npcId]` 與 `npcConfig`（視權限而定）。

5. 測試與驗證：
   - 本地啟動後端 `npm run dev`（或現有啟動指令），逐一呼叫 `/api/game/chat` 並測試含違規內容的情境，驗證自動重生或 rewrite 有效。

## 優先順序建議

1. 快取 persona（減少 I/O 並提供後續 API 支援）
2. 加入 `temperature` 至 `npcConfig` 並在 `handleGameChat` 使用
3. 實作品質不良時的重生/重寫 fallback
4. 新增 persona API 與診斷日誌
5. 中長期：集中 mission 資料與擴充測試

---

若要我直接開始實作，請告訴我你想先做哪一項（我會依序把 TODO 標記為 in-progress、實作檔案修改並附上測試步驟）。
