// NPCSelector is legacy/alternative selector UI. Neutralize to avoid unused code.
import React from "react";

const NPCSelector: React.FC<{ compact?: boolean }> = () => null;

export default NPCSelector;
