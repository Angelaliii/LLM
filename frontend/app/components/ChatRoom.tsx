// Legacy component neutralized: removed implementation because project uses new S1-S5 flows.
// This stub keeps the module present to avoid build errors during transition.
import React from "react";

const ChatRoom: React.FC = () => {
  return null;
};

export default ChatRoom;
