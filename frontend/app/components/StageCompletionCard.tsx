// StageCompletionCard neutralized as it was only used by the legacy SimpleChatRoom.
// Keep a tiny stub to avoid import-time errors if referenced elsewhere during migration.
import React from "react";

const StageCompletionCard: React.FC<any> = () => null;

export default StageCompletionCard;
